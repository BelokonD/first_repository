# Лабораторна 8

## Опис
Програма перевіряє введений рядок:
- якщо рядок непорожній — рахує кількість символів
- якщо порожній — викидає помилку

Помилки автоматично відправляються в Sentry.

## Встановлення

```bash
pip install -r lab08/requirements.txt
